{% extends 'base.html.twig' %}

{% block title %}{{ hideout.alias }} - Spy App {% endblock %}

{% block content %}
    <h1 class="text-center">Détails de la planque : {{ hideout.alias }}</h1>
    <table class="table mt-5 table-responsive">
        <thead>
        <tr>
        </tr>
        </thead>
        <tbody>
        <tr>
            <th scope="row">Alias :</th>
            <td>{{ hideout.alias }}</td>
        </tr>
        <tr>
            <th>Adresse :</th>
            <td>{{ hideout.address }}</td>
        </tr>
        <tr>
            <th>Type :</th>
            <td>{{ hideout.type }}</td>
        </tr>
        <tr>
            <th>Pays :</th>
            <td>{{ hideout.country }}</td>
        </tr>


        </tbody>
    </table>
    <div class="text-center">
        <a href="{{ path('app_hideouts_edit', { id: hideout.id })  }}" class="btn btn-warning btn-block m-2">Modifier ?</a>
        <a href="{{ path('app_hideouts') }}" class="btn btn-primary btn-block"> Retour </a>
    </div>
{% endblock %}
