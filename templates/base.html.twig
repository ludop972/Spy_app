
<!doctype html>
<html lang="fr" class="h-100">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Site de gestion de missions d'une agence d'espionnage">
    <meta name="author" content="Priant Ludovic">
    <meta name="generator" content="">
    <title>{% block title %}{% endblock %}</title>
    <!-- Bootstrap core CSS -->
    <link href="{{ '/assets/css/bootstrap.css' }}" rel="stylesheet" type="text/css">
    <link href="{{ '/assets/css/spy_app.css' }}" rel="stylesheet" type="text/css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
        .bd-placeholder-img {
            font-size: 1.125rem;
            text-anchor: middle;
            -webkit-user-select: none;
            -moz-user-select: none;
            user-select: none;
        }

        @media (min-width: 768px) {
            .bd-placeholder-img-lg {
                font-size: 3.5rem;
            }
        }
    </style>

</head>
<body class="d-flex flex-column h-100">

<header>
    <!-- Fixed navbar -->
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{path('app_home') }}"><img src="{{ '/assets/img/spy.png' }}" alt="logo_du_site(a spy)" class="spyImg"></a>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <ul class="navbar-nav me-auto mb-2 mb-md-0 float-end">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Missions</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li>
                            <a class="nav-link active" aria-current="page" href="{{ path('app_agent') }}">Agents</a>
                        </li>
                        <li>
                            <a class="nav-link active" aria-current="page" href="{{ path('app_contact') }}">Contacts</a>
                        </li>
                        <li>
                            <a class="nav-link active" aria-current="page" href="{{ path('app_target') }}">Cibles</a>
                        </li>
                        <li>
                            <a class="nav-link active" aria-current="page" href="{{ path('app_hideouts') }}">Planques</a>
                        </li>
                        <li>
                            <a class="nav-link active" aria-current="page" href="{{ path('app_specialities') }}">Spécialités</a>
                        </li>
                    {% endif %}
                </ul>
                {% if is_granted('ROLE_ADMIN') %}
                    <button class="btn btn-outline-danger"><a href="{{ path('app_logout') }}" class="login">Déconnexion</a>
                    </button>
                {% else %}
                    <button class="btn btn-outline-success"><a href="{{ path('app_login') }}" class="login">Se
                            connecter</a></button>
                {% endif %}
            </div>
        </div>
    </nav>
</header>

<!-- Begin page content -->
<main class="mt-5">
    <div class="container mt-5">
        {% block content %}
        {% endblock %}
    </div>
</main>

<footer class="footer mt-auto py-3 bg-light text-center">
    <div class="container">
        <span class="text-muted">Spy App Interface By PRIANTLudovic © 2022</span>
    </div>
</footer>

{% block javascripts %}

{% endblock %}
<script src="{{ '/assets/js/bootstrap.bundle.js' }}"></script>

</body>
</html>
